{
  "name": "chatapp-server",
  "version": "1.0.0",
  "description": "Backend server for the messaging application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx prisma generate",
    "migrate": "prisma migrate deploy",
    "build": "npx --yes prisma generate && npm run migrate",
    "prepare": "npx --yes prisma generate",
    "dev": "nodemon index.js",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "socket.io": "^4.7.4",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
